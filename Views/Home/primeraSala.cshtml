
@{
    ViewData["Title"] = "Inicio del Juego";
}


<div class="contenedor-fondoo">
    <div class="texto-superpuestoo">
        <h5>
            Estás solo y confundido @ViewBag.nombreJugador, tuviste una caída bastante fuerte.
            Solo te quedan @ViewBag.vidas corazones de vida, será mejor que tengas cuidado.
            ¿Qué vas a hacer?
        </h5>

        <p><strong>Aidvina el acertijo:</strong> Sigo tus movimientos todo el dia, mientras el sol se muestre ahí arriba</p>

        <form asp-action="VerificarAcertijo" asp-controller="Home" method="post">
            <input type="hidden" name="salaActual" value="2" />
            <input type="text" name="respuesta" placeholder="Tu respuesta..." class="form-control" required />
            <br />
            <button type="submit" class="btn btn-success">Responder</button>
        </form>

        @if (ViewBag.MensajeError != null)
        {
            <p style="color:red">@ViewBag.MensajeError</p>
        }
    </div>
</div>